import React from 'react';

// stateless functional component
// passing props in case we'll use them

const Headline = (props) => {
	return (
		<h1>Funky To Do App</h1>
	)
}

export default Headline;